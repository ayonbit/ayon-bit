{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "build": {
    "env": {
      "PRISMA_GENERATE": "true"
    },
    "commands": {
      "build": "npm run build && npx prisma generate"
    }
  }
}
